# `js/` (compatibilidad)

Los scripts principales están en [`assets/js/`](../assets/js/). Esta carpeta permite alinear el sitio con la estructura del prompt (`css/`, `js/`, `img/` en la raíz del sitio).
